/* TypeScript */

- Javascript의 타입을 엄격히 제한하기 위해 MS에서 만든 트랜스파일러(Transpiler)
- TS를 사용하면 사용할 변수, 함수, 객체 ... 에 타입을 제한할 수 있음
- TS를 사용하면 TS가 JS로 변환되는 과정에서 타입에 대한 체크가 가능
- TS를 사용하면 IDE를 통해 더 많은 타입정보를 얻을 수 있음
- TS는 터미널에서 tsc -W 명령을 통해 실시간 JS로 변환됨
- TS는 브라우져에서 직접 사용할 수 없고 JS로 변환한 후 사용함